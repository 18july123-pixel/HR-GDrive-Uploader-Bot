version: "3.8"

services:
  gdrve:
    build: .
    image: hr-gdrive-uploader:local
    ports:
      - "8080:8080"
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      PORT: 8080
    volumes:
      - ./data:/app/data
      - ./downloads:/app/downloads
